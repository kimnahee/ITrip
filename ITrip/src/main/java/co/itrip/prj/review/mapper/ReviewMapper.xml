<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.itrip.prj.review.mapper.ReviewMapper">

	<insert id="reviewConsultInsert">
	<selectKey keyProperty="reviewNo" order="BEFORE" resultType="int">
		SELECT 
			   CASE WHEN MAX(REVIEW_NO) IS NULL THEN 1 
			   ELSE MAX(REVIEW_NO)+1 
			   END 
			 FROM REVIEW
	</selectKey> 
	INSERT INTO REVIEW VALUES (#{reviewNo},'enji','소라미',#{content}, sysdate, 'consult', #{memberId}, #{star} )
	</insert>
	
	<insert id="classReviewInsert">
		<selectKey keyProperty="reviewNo" order="BEFORE" resultType="int">
			SELECT 
			   CASE WHEN MAX(REVIEW_NO) IS NULL THEN 1 
			   ELSE MAX(REVIEW_NO)+1 
			   END 
			 FROM REVIEW
		</selectKey>
			INSERT INTO REVIEW VALUES (#{reviewNo}, #{guideId}, #{nick}, #{content}, sysdate, 'class', #{memberId}, #{star} )
	</insert>
	
</mapper>